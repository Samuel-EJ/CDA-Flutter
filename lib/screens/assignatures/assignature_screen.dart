import 'package:flutter/material.dart';

class AssignatureScreen extends StatelessWidget {
  const AssignatureScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(children: [
          Text('hola mundo'),
          Text('hola mundo'),
          Text('hola mundo'),
          Text('hola mundo'),
        ],),
      ),
    );
  }
}
